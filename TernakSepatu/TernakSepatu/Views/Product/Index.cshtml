@model List<TernakSepatu.Models.Product>

@{
    ViewData["Title"] = "List of Products";
    Layout = "~/Views/Shared/Admin/_Layout.cshtml";
    int index = 1;
}

<div class="row">
    <div class="col-lg-12 mb-4">
        <!-- Simple Tables -->
        <div class="card">
            <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                <h6 class="m-0 font-weight-bold text-primary">List of Products</h6>
                <a asp-controller="Product" asp-action="Create">Create</a>
            </div>
            <div class="table-responsive">
                <table class="table align-items-center table-flush">
                    <thead class="thead-light">
                        <tr>
                            <th>No</th>
                            <th>Product Name</th>
                            <th>Category</th>
                            <th>Subcategory</th>
                            <th>Brand</th>
                            <th>Price</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var product in Model)
                        {
                            <tr>
                                <td>@index</td>
                                <td>@product.ProductName</td>
                                <td>@product.Category.CategoryName</td>
                                <td>@product.SubCategory.SubCategoryName</td>
                                <td>@product.Brand.BrandName</td>
                                <td>@product.Price</td>
                                <td style="white-space:nowrap">
                                    <a class="btn btn-primary btn-sm" asp-controller="Product" asp-action="Edit" asp-route-id="@product.Id">Edit</a>
                                    <a class="btn btn-primary btn-sm" asp-controller="Product" asp-action="Delete" asp-route-id="@product.Id">Delete</a>
                                </td>
                            </tr>
                            index++; // Increment index inside the loop
                        }
                    </tbody>
                </table>
            </div>
            <div class="card-footer"></div>
        </div>
    </div>
</div>
